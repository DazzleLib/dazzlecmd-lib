# Human Test Checklist: cli.py decomposition R6/R7 (the dispatch split)

**Target version:** dazzlecmd `0.11.29-alpha` (`0cd3d91`, branch main); lib `0.10.11-alpha` (`fae481c`) for the 2a walker tests.
**Companion automated tests:** app suite **1567** (+5 new dispatch smoke, +repointed patch-target test); byte-gate 10/10; CI flake8 selectors clean on the three touched modules.

## What this is
R6/R7 moved the READ handlers to `commands/inspect.py` and the ENTIRE dispatch fan-in to `dispatch.py` (cli.py 730→244). A pure-move refactor of the highest-risk class — **it touches every route a command can take** — and its first cut already proved the risk: `dz version` broke with all tests green (no test exercised it; flake8 F821 caught it). This checklist walks every routing family end-to-end on the live install. Reads use the real config; writes use a temp `DAZZLECMD_CONFIG`.

## High-Value Verification (~4 minutes)
- [ ] **HV.1 the resurrection case:** `dz version` AND `dz --version` — both print the version banner, exit 0. (The bug class this split created once already.)
- [ ] **HV.2 every meta route:** `dz list` · `dz info find` · `dz tree` · `dz kit` · `dz kit list` · `dz meta` — all render as before.
- [ ] **HV.3 the verb×level dispatcher:** `dz info media` (kit-level info via the tag table) · temp-config: `dz enable media` → `dz disable media` (bare-verb mutate handlers) — outputs unchanged.
- [ ] **HV.4 tool dispatch:** `dz find --help` · `dz core:safedel status` · `dz :list` (strip-and-dispatch).
- [ ] **HV.5 the intercept still front-runs:** temp-config: `dz .note=hi` → `dz .note` → `dz level=kit` → `dz level` → `dz prop delete .note` (sugar/assignment/prop paths route through the moved `_sugar_flags_hook` + dispatch_meta).
- [ ] **HV.6 verbosity plumbing:** `dz -v list` shows the meta-command diagnostic line on stderr (the moved `_init_verbosity` still fires); plain `dz list` shows none.

## Section 1: seams only a human/live run sees
- [ ] 1.1 `dz kit -h` (the generated Section-framework help) renders identically — it imports through kit_verbs, which dispatch.py now also imports (order-sensitive).
- [ ] 1.2 `dz new --help` / `dz add --help` / `dz mode status` / `dz setup find` (dry expectations: help renders, mode prints, setup runs or reports cleanly) — the R1–R3 command modules' routing through the MOVED dispatch_meta.
- [ ] 1.3 An unknown command (`dz nosuchthing`) → the argparse invalid-choice error, unchanged wording.
- [ ] 1.4 `dz enable nosuchkit` → the did-not-resolve message + nonzero exit (the bare-verb fallback path).

## What the automated tests DON'T cover
- Import-order effects at real process start (the suite imports once per session; every live `dz` is a cold import of the new module graph).
- Windows console rendering of the unchanged surfaces (byte-gate covers captured baselines only: list/info/tree/mode).
- The re-export surface under *external* one-off scripts (tests pin identity for the names the suite uses; grep `tests/one-offs/` for `from dazzlecmd.cli import` stragglers if anything acts odd).

## Cleanup
Unset `DAZZLECMD_CONFIG`; `dz list` normal.
